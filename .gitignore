# Exámenes aplicados a estudiantes (no publicar)
examenes/
examenes-ciclos-anteriores/

# Salida de construcción de Jupyter Book (regenerada por GH Actions en cada deploy)
_build/

# Configuración local de Claude Code
.claude/

# Artefactos generados al ejecutar los cuadernos (no son fuente)
contenidos/iv-procesamiento-datos-geoespaciales/ndvi_clases_gandoca.tif

# Sidecars PAM de GDAL (estadísticas/histogramas auxiliares)
*.aux.xml
